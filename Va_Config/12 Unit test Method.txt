    // ==================================================================================
    TEST_METHOD($condition=_DEBUG$)
    {
        Log("=========================================================================================");
        Log("$condition$ is running!");
        Log("-----------------------------------------------------------------------------------------");

	    // #Add test content here#
        $end$

        Log("=========================================================================================");
    } // End of $condition$(...)
    // ==================================================================================
